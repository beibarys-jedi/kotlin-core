package syntax.part3.oop.basics

class Aquarium {
    var width: Int = 20
    var height: Int = 20
    var length: Int = 20

    fun printSize() {
        println("Width: $width cm " +
                "Length: $length cm " +
                "Height: $height cm ")
    }
}